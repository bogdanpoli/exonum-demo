package selenium.pageobjects;

import org.openqa.selenium.WebDriver;
import selenium.Pages;

public class DecryptedBallotPage extends Pages {

    public DecryptedBallotPage(WebDriver driver) {
        super(driver);
    }
}
